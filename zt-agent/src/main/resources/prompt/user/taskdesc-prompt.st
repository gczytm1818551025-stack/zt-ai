子任务[{{index}}]的内容是："{{taskNode}}"
上一子任务情况：{{latestResult}}

## 完成子任务时的要点(重要)
1.你仅可以根据指定的单个技能完成当前子任务。
2.本次调用工具仅仅是为了完成当前子任务，禁止做其他任何无关的事情且禁止规划下一子任务。
3.如果决定调用工具，则必须检查每个工具调用所需的所有参数，禁止缺漏任何参数！且参数类型必须与技能描述中指定的数据类型完全一致！！
4.如果任务内容中有结束总任务的明确内容，则当前子任务必须且仅可以调用done动作结束任务！！

## 指定技能
{{skill}}
